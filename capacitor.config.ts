import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.movieApp',
  appName: 'MovieApp',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
